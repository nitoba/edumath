import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:edumath/app/modules/challenge/challenge_page.dart';

void main() {
  testWidgets('ChallengePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChallengePage(title: 'Challenge')));
    //  final titleFinder = find.text('Challenge');
    //  expect(titleFinder, findsOneWidget);
  });
}
