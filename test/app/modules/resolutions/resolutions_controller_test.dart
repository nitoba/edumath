import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:edumath/app/modules/resolutions/resolutions_module.dart';

void main() {
  initModule(ResolutionsModule());
  // ResolutionsController resolutions;
  //
  setUp(() {
    //     resolutions = ResolutionsModule.to.get<ResolutionsController>();
  });

  group('ResolutionsController Test', () {
    //   test("First Test", () {
    //     expect(resolutions, isInstanceOf<ResolutionsController>());
    //   });

    //   test("Set Value", () {
    //     expect(resolutions.value, equals(0));
    //     resolutions.increment();
    //     expect(resolutions.value, equals(1));
    //   });
  });
}
